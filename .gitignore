# Собранные лиды — это данные, не код. Публичный репозиторий держит только
# процесс (см. правило "GitHub = процесс, не данные", уже применённое к
# RUSIMEX и pc-price-tracker в этом портфеле).
data/
out/
*.sqlite3
*.xlsx

# Исходящие материалы (КП и т.п.) — не для публичного репозитория
outreach/

# Секреты
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.venv/
venv/

# Claude Code local machine-specific permissions
.claude/settings.local.json
